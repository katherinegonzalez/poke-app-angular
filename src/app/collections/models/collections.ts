import { IPokemon } from 'src/app/poke-main/models/interfaces/pokemon';

export class Collection {
  nameCollection: string;
  descriptionCollection: string;
  pokemonsCollection: IPokemon[];

  constructor() { }
}
